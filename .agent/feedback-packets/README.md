# Feedback Packets

Create one Markdown file per user correction, review finding, or design concern that should affect future work. Include the observation, its impact, and the required response. Remove links from `../feedback.md` when a packet is resolved.
